#!/usr/bin/env bash
# nickserv.sh — NickServ and OPER registration with retry logic.
#
# Sourced by setup.sh; do not execute directly.
# Requires: common.sh (for info, success, warn, err, PROJECT_DIR)
#
# Placeholder — populated in Task 2.
