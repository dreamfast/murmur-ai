# Copy to .env and fill in your values.
# The vault passphrase encrypts your secrets at rest.
MURMUR_VAULT_PASS=changeme

# Only needed for the setup script. After setup, the key is stored in the vault.
LLM_API_KEY=sk-or-v1-your-openrouter-key-here
